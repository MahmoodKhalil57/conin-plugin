{
  "name": "conin-plugin",
  "private": true,
  "description": "Conin marketplace plugin — projected from conin's x-suluk-agents contract via @suluk/agents (no hand-authored artifacts).",
  "scripts": {
    "build": "bun scripts/build-plugin.ts"
  },
  "dependencies": {
    "@suluk/agents": "^0.1.3",
    "@suluk/core": "^0.1.11"
  }
}
